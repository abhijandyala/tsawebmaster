import { PublicEntryLayout } from '@/components/layout/PublicEntryLayout';

export default function PublicLayout({ children }: { children: React.ReactNode }) {
  return <PublicEntryLayout>{children}</PublicEntryLayout>;
}
